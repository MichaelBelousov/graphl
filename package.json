{
	"scripts": {
		"test": "npm run test:node && npm run test:wasm",
		"test:node": "cd lang-lib && node --test -r @swc-node/register -r node-zigar/cjs ./tests/test.cts",
		"test:wasm-build": "cd lang-lib && zig test --test-filter wasm-test-entry --test-no-exec -femit-bin=./zig-out/bin/wasm-test.wasm -target wasm32-wasi ./src/wasm-test.zig",
		"test:wasm": "npm run test:wasm-build && cd lang-lib && wasmtime ./zig-out/bin/wasm-test.wasm"
	},
	"dependencies": {
		"zigar-runtime": "^0.13.2"
	},
	"devDependencies": {
		"@swc-node/register": "^1.10.9",
		"@types/node": "^22.7.5",
		"node-zigar": "^0.13.2"
	}
}
